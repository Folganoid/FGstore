<div class="col-xs-1 col-md-3"></div>
<div class="col-xs-10 col-md-6">
    <h2 align="center">Common Category list</h2>
    <br>
    <ul>
        {% for cat in cats %}
            <li><b>{{ cat['name'] }}</b> ({{ cat['notice'] }})
                <a class="pull-right"
                   href="{{ twigLink('item_category', {'id': cat['id']}) }}">view...</a>
            </li>
        {% endfor %}
    </ul>
</div>
<div class="col-xs-1 col-md-3"></div>